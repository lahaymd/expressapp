
p testing ajax with express

-date = new Date()
-hour = date.getHours()

-if ((hour >= 6) && (hour <= 17)){
	h3 Day Time
-} else {
	h3 Night Time
-}

mixin nflPlayer(name, pos, team)
	li #{name} is the #{pos} for the #{team}

ul#nflPlayers
	+nflPlayer("Jake", "Quaterback", "Bears")


mixin copyr
	| &#169;

p
	+copyr
	| 2016

mixin makeList()
	ul
		-args = Array.prototype.slice.call(arguments);
		for item in args
			li= item

+makeList('dog', 'cat', 'fish')