div(ng-controller= 'dataController')
  input(type='text' ng-model='test')
  p Create new profile {{mongoStuff.length}}
  ul  
   li(ng-repeat='stuff in arrFromMyObj  | orderBy : "-name" | filter: test ') {{stuff.name | uppercase}} {{stuff.password}} 
